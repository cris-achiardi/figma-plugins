---
const steps = [
  {
    number: '01',
    title: 'Connect your library',
    description: 'Install the Figma plugin and connect it to your component library. The plugin automatically discovers all components.',
  },
  {
    number: '02',
    title: 'Track changes',
    description: 'Every time a component is modified, the plugin detects the diff. Added properties, removed variants, resized layers, to create a version entry.',
  },
  {
    number: '03',
    title: 'Publish versions',
    description: 'Review diffs, approve changes, and publish with semantic version bumps. Your entire team gets a clear, traceable changelog.',
  },
];
---

<section class="how-it-works">
  <div class="two-col">
    <div class="col-image">
      <img src="/assets/img/plugin-mocks.png" alt="Component Changelog plugin interface" class="mockup" />
    </div>
    <div class="col-steps">
      <h2 class="section-label">How it works</h2>
      <div class="steps">
        {steps.map((s) => (
          <div class="step">
            <span class="step-number">{s.number}</span>
            <div class="step-content">
              <h3>{s.title}</h3>
              <p>{s.description}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>

<style>
  .how-it-works {
    padding: 6rem 2rem;
    max-width: 960px;
    margin: 0 auto;
  }

  .two-col {
    display: flex;
    align-items: center;
    gap: 4rem;
  }

  .col-image {
    flex: 4;
    min-width: 0;
  }

  .mockup {
    width: 100%;
    height: auto;
    object-fit: contain;
    border-radius: var(--radius-lg);
  }

  .col-steps {
    flex: 3;
    min-width: 0;
  }

  .section-label {
    font-family: var(--font-mono);
    font-size: 12px;
    font-weight: 500;
    color: var(--text-muted);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    margin-bottom: 2rem;
  }

  .steps {
    display: flex;
    flex-direction: column;
    gap: 2.5rem;
  }

  .step {
    display: flex;
    align-items: flex-start;
    gap: 1.25rem;
  }

  .step-number {
    font-family: var(--font-mono);
    font-size: 14px;
    font-weight: 600;
    color: var(--text-muted);
    flex-shrink: 0;
    padding-top: 2px;
  }

  .step-content h3 {
    font-family: var(--font-sans);
    font-size: 16px;
    font-weight: 600;
    color: var(--text-primary);
    margin-bottom: 0.5rem;
  }

  .step-content p {
    font-size: 14px;
    color: var(--text-secondary);
    line-height: 1.6;
  }

  @media (max-width: 768px) {
    .two-col {
      flex-direction: column;
      gap: 2.5rem;
    }

    .how-it-works {
      padding: 4rem 1.5rem;
    }
  }
</style>
