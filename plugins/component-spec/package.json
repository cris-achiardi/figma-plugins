{
  "name": "component-spec",
  "version": "1.0.0",
  "description": "Export a compact, AI-friendly spec of a Figma component — API, tokens, and variant deltas as plain text.",
  "main": "code.js",
  "scripts": {
    "build": "npx esbuild code.ts --bundle --outfile=code.js --target=es2017 && npx esbuild ui.tsx --bundle --outfile=ui.js --target=es2017 && node inline-ui.js"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.121.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "esbuild": "^0.27.1",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
